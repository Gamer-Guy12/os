/// Here the memory map is discovered

#include <stddef.h>
#include <pmm.h>

void move_to_memory_map(uint8_t** memPtr) {
    // Increment the pointer to skip over until ou get to the start of the memory map
}

uint8_t* get_memory_map(void) {
    return NULL;
}
